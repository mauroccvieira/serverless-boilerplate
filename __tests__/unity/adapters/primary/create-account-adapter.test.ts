import { main } from "@adapters/primary/create-account/handler";

describe("some text", () => {
  it("some test", () => {
    expect(main).toBeDefined();
  });

  it("some test 2", () => {
    expect(main).toBeDefined();
  });
});
