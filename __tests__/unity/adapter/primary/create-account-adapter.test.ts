import { handler } from "@adapters/primary/create-account-adapter";


describe("some text", () => {
	it("some test", () => {
		expect(handler).toBeDefined();
	});
  
	it("some test 2", () => {
		expect(handler).toBeDefined();
	});
});
